# recorded telemetry (bind-mounted into the container)
data/

# local compose overrides (LS_KEEP_DISCARDED capture toggle)
.env

# python
__pycache__/
*.pyc
.venv/

# PyInstaller build output (the exe is published via GitHub Releases, not tracked)
build/
dist/

# editors / os
.vscode/
.idea/
Thumbs.db
.DS_Store
